# Base origin of the Masterwork backend (FastAPI). No trailing slash.
# The generated client bakes the /api/v1 prefix into every request path,
# so this must be the ORIGIN only.
VITE_API_URL=http://localhost:8008
